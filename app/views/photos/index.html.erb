<% if @photos.any? %>
	<% @photos.each do |photo| %>
		<section class="photo">
			<%= link_to image_tag(photo.image.url(:med)), photo.image.url %>
			<%= photo.caption %>
		</section>
	<% end %>
<% else %>
	No photos (yet).
<% end %>
<%= link_to 'Add a photo', new_photo_path %>